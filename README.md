# A simple bot for Telegram running on a RaspberryPI

### Requirements

### Installation
```
sudo pip install python-telegram-bot
sudo pip install ADAFruit_GPIO
sudo pip install RPi.GPIO
```

### Features